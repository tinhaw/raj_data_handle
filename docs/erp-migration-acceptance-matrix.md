# ERP 迁移与融合验收矩阵

基线日期：2026-08-27

对照来源：云端 `erp.aiggtj.com` 只读巡检及本地最新 `raj-ads-manage` 源码。
目标系统：`raj_data_handle`，沿用一套登录体系和一套 `SourceConfig + RemoteAccount` 盘口/账号主数据。

状态定义：

- **功能等价**：页面、API、权限、数据、导入导出和状态机与线上版本逐项对比通过。
- **部分等价**：已有页面或服务，但尚未完成线上行为和视觉差异验收。
- **代码就绪/执行禁用**：远端协议和内部执行代码已迁移，但没有可从页面或公开 API 触发的执行入口。
- **待授权执行**：不是代码缺口；必须另行确认生产备份、回退、窗口或具体远端业务操作后才能执行。

## 功能验收

| 云端 ERP 能力 | 融合实现 | 权限与范围 | 自动化验收 | 状态 |
| --- | --- | --- | --- | --- |
| 工作台 | 当日 KPI、7 日趋势、草稿/负结余/锁账/导入错误、最近日结 | `ERP_WORKSPACE_VIEW`；按公司范围汇总 | 服务测试 + 线上视觉基线 | 部分等价 |
| 投放公司/投放线 | 原 Spring 服务与页面已迁入；0036 提供原字段兼容表和当前 UUID 数据转换 | `ERP_OPERATOR_VIEW/MANAGE`；公司范围经 0035 数字 ID 映射 | Spring 全量测试 + 迁移样例测试；页面差异待测 | 部分等价 |
| 连续日台账 | 原 Spring 自动承接、计算/影响预览、批量保存、确认和重开服务已指向 0036 兼容表 | 查看、录入、复核、越权覆盖分别授权；按映射公司范围 | Spring 全量测试 + 迁移样例测试；API 差异待测 | 部分等价 |
| 月结锁账 | 原 Spring 月度校验、锁定、解锁和写入阻断服务已指向 0036 兼容表 | `ERP_PERIOD_LOCK`；按映射公司范围 | Spring 全量测试；API 差异待测 | 部分等价 |
| 导入中心 | 原 Spring 粘贴/XLSX、模板、预览、提交、源文件和错误报告服务已指向 0036 兼容表 | `ERP_IMPORT_VIEW/EXECUTE`；任务按公司范围过滤 | Spring 全量测试；XLSX 差异待测 | 部分等价 |
| 汇总报表 | 日/月、公司/投放线/币种/状态筛选，原币种/名义 U、趋势图、Excel | `ERP_REPORT_VIEW/EXPORT`；按公司范围 | 服务测试 + XLSX/视觉差异测试 | 部分等价 |
| 审计日志 | 追加式记录、日期/动作/公司筛选、详情、请求 ID | `ERP_AUDIT_VIEW`；受限用户只见可关联到授权公司的记录 | 服务测试 + 页面差异测试 | 部分等价 |
| 用户与权限 | 一个本地用户多 ERP 角色，角色权限与公司范围分离 | `ERP_ACCESS_MANAGE` | 服务测试 + 页面差异测试 | 部分等价 |
| 系统设置 | ERP 时区、精度/舍入、上传上限等基线展示；会话仍复用主系统设置 | 系统管理权限 | API/页面差异测试 | 部分等价 |
| 盘口与远端账号 | 复用 `SourceConfig` 盘口和统一 `RemoteAccount` | 管理、查看分离；凭据永不回传 | 服务测试 + 旧 API 兼容测试 | 部分等价 |
| 标签和兑换档位预设 | 每账号标签快照、预设、同步及变化提示 | 读取、同步和管理分别授权 | 服务测试 + 旧 API 兼容测试 | 部分等价/执行禁用 |
| 多盘口兑换任务组 | 线上同源页面和 Spring 状态机已接入；0037 保留任务组、选择顺序、盘口子任务及 Long ID | `ERP_REDEMPTION_GENERATE`；账号还需 `ERP_REDEMPTION_CREATE` | Spring 回归 + 0037 转换测试；生产数据差异待测 | 部分等价 |
| 兑换远端状态机 | 单项创建、组发布、下载、取消、失败、恢复、并发占用、执行历史 | 每个操作同时校验账号能力和本次显式授权 | runner + 旧 API 契约测试 | 代码就绪/执行禁用 |
| 单/多盘口 Excel | 单盘口和任务组多 Sheet 导出与线上文件内容一致 | `ERP_REDEMPTION_EXPORT` | XLSX 单元格差异测试 | 部分等价 |
| 当前远端 HTTP 协议 | 登录、连接检测、标签、创建、列表定位、发布、取消、Excel 下载；ERP 时区为 `Asia/Shanghai` | 适配器要求与账号/盘口/操作完全匹配的 grant | `test_erp_redemption_remote_http_adapter.py` | 代码就绪/执行禁用 |
| 远端执行器 | 只从统一账号解密凭据；令牌仅驻留内存；立即发布失败按 15/30/60 分钟回退 | 内部 runner 要求 `execution_authorized=True`；没有 HTTP/定时入口 | gate、adapter、plan 测试 | 代码就绪/执行禁用 |

## 数据与切换验收

| 项目 | 当前结果 | 状态 |
| --- | --- | --- |
| 数据库迁移链 | 0028–0037 已编写；全新 SQLite 已升级到 0037 head，0035→0036 核心转换及 0036→0037 兑换状态转换通过；0035–0037 尚未执行生产 | 已完成（迁移代码） |
| ERP 历史数据导入 | 2026-08-27 生产一致性备份已导入本机全新 0037 隔离库；生产 `data_handle` 未写入 | 隔离演练通过；生产待执行 |
| P5 隔离演练工具 | 真实旧 ERP V19 与真实 `data_handle` 0034 生产备份已在隔离克隆完成 0035–0037 + 十二表导入；数量/金额/摘要一致、硬孤儿为 0、sequence 正常，原分析表无非预期行数变化，`alembic check` 无漂移 | 真实双快照演练通过；生产待执行 |
| 生产 RDS schema | 尚未执行；目标只能是 `data_handle` | 待确认备份、回退方案和窗口 |
| 应用发布 | 尚未上传、重启或切流 | 待生产发布授权条件完整 |
| 远端连接检测/标签同步 | 本轮未调用 | 待逐项授权 |
| 远端创建/发布/取消/下载 | 本轮未调用，页面和公开 API 无执行入口 | 待逐项授权及联调 |

## 结论

当前已经存在本地 ERP 模型、统一身份/账号模型和部分远端协议代码，但尚未达到线上 ERP 的页面、API 与行为等价。迁移以 [线上 ERP 高保真迁移执行方案](./erp-online-parity-migration.md) 和机器基线为准；在逐项差异测试通过前，不得宣称完整迁移完成。
