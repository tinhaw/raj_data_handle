# 提现订单 Excel 导出字段映射核对表

本表参照[充值订单 Excel 导出字段映射核对表](充值订单字段对照表.md)编写，供确认提现订单页的本地缓存、明细展示和渠道汇总口径。它只描述远端管理后台**提现订单 Excel 导出**的安全白名单；页面查询只读取本地缓存，不会在用户打开或翻页时请求远端。

## 1. 刷新与安全边界

- 每个盘口按其业务时区导出一个完整自然日；原始 Excel 仅在内存中解析，成功后立即丢弃，不保存文件、完整响应或未白名单列。
- 自动任务每日业务时区 `00:05:01` 运行，默认导出前一天 `00:00:00` 至 `23:59:59`；系统配置也可改为一次性的指定日期，成功后不在次日重复执行。
- 手动刷新可选前天、昨天、今天，默认昨天，范围同样是所选自然日的完整业务时区窗口。
- 全部白名单行校验、状态识别和去重成功后，才写入或覆盖该盘口该日期的本地缓存；任何解析、认证或状态映射错误都保留原有缓存。
- 订单以 `source_id + remote_order_id` 去重；同一导出中重复主键只保留最终一行，并记录导出行数、导入数和重复数。

## 2. Excel 安全白名单与字段对照

使用方式中，**直接展示**表示在“提现订单明细”显示；**间接引用**表示用于筛选、分组、统计或排序。金额以十进制文本入库，由服务端按精确数值计算。

| 提现 Excel 导出列 | 参照的充值字段 / 用途 | 本地缓存字段 / 明细 API 字段 | 提现订单明细 | 支付渠道汇总 | 转换与校验 | 确认状态 |
| --- | --- | --- | --- | --- | --- | --- |
| 主键 | 充值“订单id” | `remote_order_id` / `id` | **直接展示**；**间接引用**（去重） | **间接引用**（订单数） | 必填；按盘口 + 主键唯一。空值使本次导入失败。 | 待确认 |
| 提现uid | 充值“用户uid” | `uid` / `uid` | **直接展示**；**间接引用**（精确筛选） | 不引用 | 保存导出原值。 | 待确认 |
| 提现订单号 | 充值“我方订单号” | `order_num` / `orderNum` | **直接展示**；**间接引用**（包含筛选） | 不引用 | 保存导出原值。 | 待确认 |
| 用户渠道 | 充值“用户渠道” | `channel` / `channel` | **直接展示**；**间接引用**（筛选） | 不引用 | 保存导出原值。 | 待确认 |
| 三方支付订单号 | 充值“三方支付订单号” | `out_trade_no` / `outTradeNo` | **直接展示**；**间接引用**（包含筛选） | 不引用 | 空、`0`、`-` 统一视为无值。 | 待确认 |
| 支付通道名称 | 充值“支付渠道名称” | `pay_channel_name` / `payChannelName` | **直接展示** | **间接展示**（渠道显示名） | 以导出值优先；可由当前盘口字典补充展示。 | 待确认 |
| 支付通道 | 充值“支付渠道” | `pay_channel` / `payChannel` | **直接展示**；**间接引用**（筛选） | **间接引用**（分组键） | 渠道代码与名称共同保存；分组以代码为准。 | 待确认 |
| 提现金额 | 充值“支付金额” | `amount` / `amount` | **直接展示** | 不作为成功金额 | 只作订单金额展示与明细核对，不替代到账金额。 | 待确认 |
| 提现手续费 | 充值无对应字段 | `fee` / `fee` | **直接展示** | **间接展示**（成功手续费） | 仅状态为“代付成功”时参与手续费合计。 | 已确认口径 |
| 到账金额 | 充值“支付金额” | `real_amount` / `realAmount` | **直接展示** | **间接展示**（代付成功金额） | 仅状态为“代付成功”时参与成功金额合计。 | 已确认口径 |
| 是否首提 | 充值“是否首充” | `is_first` / `isFirst` | **直接展示**；**间接引用**（筛选） | 不引用 | 保存导出原值，不自行推断。 | 待确认 |
| 状态 | 充值“订单状态” | `status`、`status_label` / `status`、`statusLabel` | **直接展示**；**间接引用**（状态筛选） | **间接引用**（成功、失败、已提交、卡单率） | 保留原始中文状态；按当前盘口的 `withdraw_status` 字典映射为稳定代码。未知或歧义状态使本次导入失败，不写入不确定数据。 | **已确认：各盘口字典完整覆盖导出状态** |
| 创建时间 | 充值“创建时间” | `create_time`、`create_time_utc` / `createTime` | **直接展示**；**间接引用**（时间筛选、排序） | **间接引用**（日期分组） | 按盘口业务时区解析为 UTC 查询值；页面显示导出墙上时间。 | 待确认 |
| 提交时间 | 充值“支付时间” | `submit_time` / `submitTime` | **直接展示** | 不引用 | 仅作明细时间线；不得与完成时间互换。 | 待确认 |
| 修改时间 | 充值“完成时间” | `update_time` / `updateTime` | **直接展示** | 不引用 | 页面名称固定为“完成时间”，展示修改时间原值。 | **已确认** |
| 审核人 | 充值无对应字段 | `audit_admin` / `auditAdmin` | **直接展示**；**间接引用**（操作人员筛选） | 不在渠道汇总中使用 | 用于独立的“操作人员汇总”，空值显示为“系统”。 | 待确认 |

### 明确禁止读取、缓存和导出的列

即使远端 Excel 含有下列字段，解析器也不读取其单元格，数据库、日志、页面 API、CSV/Excel 导出均不得出现：

- 银行卡号、银行卡账户、收款人姓名；
- 手机号、银行名称、银行代码、IFSC；
- 失败原因、备注等自由文本原因；
- IP、设备信息、完整远端响应或完整 Excel 原文件；
- 任何未列入上表安全白名单的列。

## 3. 页面字段与分组

页面采用三个独立标签：`提现订单明细`、`支付渠道汇总`、`操作人员汇总`。前两者按创建时间和支付通道分析，操作人员汇总按审核人和状态统计；三者都只读本地缓存。

渠道汇总的一行固定按下列键分组：

```text
source_id + 创建日期（盘口业务时区） + pay_channel
```

`pay_channel_name` 是显示名，缺失时使用渠道代码。相同渠道代码不能因显示名变化拆为多行。

## 4. 渠道汇总指标口径

分母均限定在**同一盘口、同一创建日期（盘口业务时区）**的本地缓存订单；所有数量均按去重后的 `remote_order_id` 计算，所有金额均由服务端以精确十进制求和。分母为零时显示 `—`，不显示 `0%`。

| 汇总列 | 依赖字段 | 计算公式 |
| --- | --- | --- |
| 日期 | 创建时间 | 创建时间换算到盘口业务时区后的自然日。 |
| 支付渠道名称 | 支付通道名称、支付通道 | 显示 `pay_channel_name`，缺失时显示 `pay_channel`。 |
| 提款总订单数 | 主键 | 当前渠道所有去重订单数。 |
| 代付成功订单数 | 主键、状态 | 当前渠道状态为“代付成功”的去重订单数。 |
| 代付成功金额 | 到账金额、状态 | `sum(real_amount)`，仅状态为“代付成功”。 |
| 提款总手续费 | 提现手续费、状态 | `sum(fee)`，仅状态为“代付成功”。 |
| 代付失败数 | 主键、状态 | 当前渠道状态为“代付失败”的去重订单数。 |
| 已提交代付数 | 主键、状态 | 当前渠道状态为“已提交代付”的去重订单数。 |
| 审核拒绝数（补充） | 主键、状态 | 当前渠道状态为“审核拒绝”的去重订单数；单列展示，避免与代付失败混淆。 |
| 渠道代付成功订单量占比 | 主键、状态、支付通道 | 当前渠道代付成功订单数 ÷ 当天全部渠道代付成功订单数。 |
| 渠道代付成功金额占比 | 到账金额、状态、支付通道 | 当前渠道代付成功金额 ÷ 当天全部渠道代付成功金额。 |
| 卡单率 | 主键、状态 | 当前渠道已提交代付数 ÷ 当前渠道提款总订单数。 |
| 代付成功率（补充） | 主键、状态 | 当前渠道代付成功订单数 ÷ 当前渠道提款总订单数。 |

图表、指标卡、渠道表和导出必须复用同一个服务端聚合结果，前端不得重新计算上述指标。页面悬停图表时可显示渠道名称和值；圆弧引线标签仅显示名称，避免金额文字挤占图面。

## 5. 已确认与待人工确认项

已确认：

1. “修改时间”即提现订单的完成时间，页面固定显示为“完成时间”。
2. 各盘口 `withdraw_status` 字典完整覆盖导出中的全部状态文案；导入时仍会校验同一文案不会歧义对应多个代码。

仍待人工确认：

1. 远端 Excel 列名与上表完全一致，尤其是“主键”“支付通道”“到账金额”“修改时间”。
2. 是否需要在明细页增加“审核拒绝数”和“代付成功率”以外的业务指标；未确认前不增加敏感字段。
